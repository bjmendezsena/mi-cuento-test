model Task {
    id          String @id
    title       String
    description String
    dueDate     String
    priority    Int

    updatedAt DateTime @updatedAt
    createdAt DateTime @default(now())
}
